import type { FastifyPluginAsync } from 'fastify';

/**
 * todosRoutes — Fastify plugin for the /todos resource.
 *
 * Routes will be implemented in Stories 2.1 and 2.2.
 * Do NOT wrap with fastify-plugin — route encapsulation is intentional.
 */
const todosRoutes: FastifyPluginAsync = async () => {
  // Routes will be registered here in Stories 2.1 and 2.2
};

export default todosRoutes;
